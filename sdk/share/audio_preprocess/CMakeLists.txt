add_definitions(
        -DWEBRTC_POSIX
        -DAECM_WITH_SR_APPROX
        -DFIXED_POINT
        -DGramSchmidt
        -DUSE_BSSA
        -DBIN_512
        -DMVDR
        )

include_directories(
.
)
